---
name: {{slug}}
description: DRAFT — {{task_description}}
triggers: [{{triggers}}]
---

# {{slug}}

> Draft skill generated by claude-omakase. Iterate with Claude to make this concrete.

## What this skill does

{{task_description}}

## When to activate

Claude should use this skill when the user is doing any of:

{{triggers_bullets}}

## Steps

1. Check preconditions before running anything:
   ```bash
   command -v <tool> || { echo "<tool> not found"; exit 1; }
   ```
2. Announce what is about to change (files written, services restarted, etc.) and confirm with the user if destructive.
3. Execute the steps:
   ```bash
   # Step 1 — (TODO) describe what this does
   <command-1>

   # Step 2 — (TODO) describe what this does
   <command-2>
   ```
4. Verify success (exit codes, file existence, expected output).
5. (TODO) Describe success criteria — what confirms it worked.

## Rollback

(TODO) Describe how to undo these steps, or note "steps are idempotent — safe to re-run."

## Examples

**User:** (TODO) Paste a real example request here.

**Claude:** (TODO) Show the commands you'd run and the expected output.

---

_Generated {{generated_at}} by claude-omakase. Replace TODO items before relying on this skill._

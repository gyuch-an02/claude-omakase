---
name: my-shell-automation-skill
description: DRAFT — replace with a one-line description of what this automation does.
triggers: ["automate", "bash script", "shell task", "run automation"]
---

# my-shell-automation-skill

## What this skill does

Runs a shell automation sequence: validates preconditions, executes steps in order, and reports what changed.

Replace this with a concrete description of the exact automation and its expected side effects.

## When to activate

Use this skill when the user:

- Asks to run a recurring shell task by name
- Mentions the workflow: `<workflow-name-here>`
- Says "do the usual X" where X is this automation

## Steps

1. Check preconditions before running anything:
   ```bash
   # Example: required tool present?
   command -v <tool> || { echo "<tool> not found"; exit 1; }
   ```
2. Announce what is about to change (files written, services restarted, etc.) and confirm with the user if destructive.
3. Execute the steps:
   ```bash
   # Step 1 — describe what this does
   <command-1>

   # Step 2 — describe what this does
   <command-2>
   ```
4. Verify success (exit codes, file existence, expected output).
5. Report a one-line summary: what ran, what changed, any warnings.

## Rollback

If step N fails:
- Describe how to undo steps 1…N-1 here.
- Or note "steps are idempotent — safe to re-run."

## Examples

**User:** Run the nightly data sync

**Claude:** Syncing data…
```bash
./scripts/sync.sh --source s3://bucket/data/ --dest data/
```
Synced 42 new files (1.2 GB). No errors.

---

_Generated by claude-omakase. Edit this file to match your actual automation._

---
name: my-python-cli-skill
description: DRAFT — replace with a one-line description of what this skill does.
triggers: ["run script", "python script", "execute python"]
---

# my-python-cli-skill

## What this skill does

Runs a Python CLI tool or script on behalf of the user, handles common error patterns, and surfaces output cleanly.

Replace this with a concrete description of the exact script/tool and what success looks like.

## When to activate

Use this skill when the user:

- Asks to run a Python script by name
- Wants to process files with a specific Python tool
- Mentions the tool name: `<tool-name-here>`

## Steps

1. Confirm the target script path and any required arguments with the user.
2. Check that the virtual environment is active or dependencies are installed:
   ```bash
   python -m pip show <package> 2>/dev/null | grep Version || echo "not installed"
   ```
3. Run the script, capturing stdout and stderr:
   ```bash
   python <script.py> <args>
   ```
4. If exit code != 0, surface the error with context. Common fixes:
   - Missing dependency → `pip install <package>`
   - Wrong Python version → check `python --version`
5. Show the user the output or output file path when done.

## Examples

**User:** Run the data cleaner on `data/raw.csv`

**Claude:** Running `clean.py` on `data/raw.csv`…
```bash
python scripts/clean.py data/raw.csv --output data/clean.csv
```
Done. Output at `data/clean.csv` (1,234 rows, 0 errors).

---

_Generated by claude-omakase. Edit this file to match your actual script._

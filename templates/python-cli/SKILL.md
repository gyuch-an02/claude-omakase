---
name: {{slug}}
description: DRAFT — {{task_description}}
triggers: [{{triggers}}]
---

# {{slug}}

> Draft skill generated by claude-omakase. Iterate with Claude to make this concrete.

## What this skill does

{{task_description}}

## When to activate

Claude should use this skill when the user is doing any of:

{{triggers_bullets}}

## Steps

1. Confirm the target script path and any required arguments.
2. Check that the virtual environment is active or dependencies are installed:
   ```bash
   python -m pip show <package> 2>/dev/null | grep Version || echo "not installed"
   ```
3. Run the script:
   ```bash
   python <script.py> <args>
   ```
4. If exit code != 0, surface the error. Common fixes:
   - Missing dependency → `pip install <package>`
   - Wrong Python version → check `python --version`
5. (TODO) Describe success criteria — what output or file confirms it worked.

## Examples

**User:** (TODO) Paste a real example request here.

**Claude:** (TODO) Show the command you'd run and the expected output.

---

_Generated {{generated_at}} by claude-omakase. Replace TODO items before relying on this skill._

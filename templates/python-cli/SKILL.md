---
name: {{slug}}
description: {{task_description}}
triggers: [{{triggers}}]
generated_at: {{generated_at}}
---

# {{slug}}

## What this skill does

{{task_description}}

## When to activate

Use this skill when the user:

{{triggers_bullets}}

## Steps

1. Confirm the target script path and any required arguments with the user.
2. Check that the virtual environment is active or dependencies are installed:
   ```bash
   python -m pip show <package> 2>/dev/null | grep Version || echo "not installed"
   ```
3. Run the script, capturing stdout and stderr:
   ```bash
   python <script.py> <args>
   ```
4. If exit code != 0, surface the error with context. Common fixes:
   - Missing dependency → `pip install <package>`
   - Wrong Python version → check `python --version`
5. Show the user the output or output file path when done.

## Examples

**User:** [describe a concrete user request here]

**Claude:** Running…
```bash
python <script.py> <args>
```
Done. [describe the result]

---

_Generated by claude-omakase. Edit this file to replace placeholder steps with your actual script._

---
name: {{slug}}
description: {{task_description}}
triggers: [{{triggers}}]
generated_at: {{generated_at}}
---

# {{slug}}

## What this skill does

{{task_description}}

## When to activate

Use this skill when the user:

{{triggers_bullets}}

## Steps

1. Confirm the script name and any environment variables needed.
2. Check `node_modules` exists, install if missing:
   ```bash
   [ -d node_modules ] || npm install
   ```
3. Run the script:
   ```bash
   node <script.js>
   # or
   npm run <script-name>
   ```
4. If the script fails, check for:
   - Node version mismatch → `node --version` vs `engines` in `package.json`
   - Missing env vars → check for `.env.example`
   - Port already in use → `lsof -i :<port>`
5. Report output path or result summary to the user.

## Examples

**User:** [describe a concrete user request here]

**Claude:** Running…
```bash
npm run <script-name>
```
Done. [describe the result]

---

_Generated by claude-omakase. Edit this file to replace placeholder steps with your actual script._

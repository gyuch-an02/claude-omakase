---
name: my-node-script-skill
description: DRAFT — replace with a one-line description of what this skill does.
triggers: ["run node", "node script", "npm run", "execute script"]
---

# my-node-script-skill

## What this skill does

Runs a Node.js script or npm task, manages common dependency issues, and surfaces results clearly.

Replace this with a concrete description of the exact script and what it produces.

## When to activate

Use this skill when the user:

- Asks to run a specific npm script by name
- Wants to execute a Node.js utility
- Mentions the script name: `<script-name-here>`

## Steps

1. Confirm the script name and any environment variables needed.
2. Check `node_modules` exists, install if missing:
   ```bash
   [ -d node_modules ] || npm install
   ```
3. Run the script:
   ```bash
   node <script.js>
   # or
   npm run <script-name>
   ```
4. If the script fails, check for:
   - Node version mismatch → `node --version` vs `engines` in `package.json`
   - Missing env vars → check for `.env.example`
   - Port already in use → `lsof -i :<port>`
5. Report output path or result summary to the user.

## Examples

**User:** Run the catalog build

**Claude:** Building catalog…
```bash
npm run build:catalog
```
Done. `catalog.json` updated — 142 entries from 3 adapters.

---

_Generated by claude-omakase. Edit this file to match your actual script._
